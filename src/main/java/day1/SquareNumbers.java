package day1;

public class SquareNumbers {
	
	/*
	 * Problem : Given sorted integer array in non decreasing order, 
	 * return the array of square of each number sorted in non decreasin order
	 * 
	 * 
	 * PseudoCode
	 * 
	 * 1. Get the input array
	 * 2. Traverse through the array and multiple the number by itself and store in the same location
	 * 3. Finally, sort the array
	 * 4. Print & return
	 * 
	 */

}
